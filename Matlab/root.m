%This is a Matlab SCRIPT.
%It doesn't clear the variables in the workspace
%This is very dangerous if you run it several times - variables can get
%left over from the previous run, and cause errors.

complexFunction;